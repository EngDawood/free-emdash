---
import type { Locale } from "../../i18n/utils";
import { t } from "../../i18n/utils";
import { skillsData } from "../../data/skills";

interface Props {
	locale: Locale;
}

const { locale } = Astro.props;
---

<section id="skills" class="skills reveal">
	<header class="section-header reveal-child">
		<h2 class="section-label">{t("skills.section", locale)}</h2>
		<h3 class="section-title">{t("skills.heading", locale)}</h3>
	</header>
	<div class="skills-grid">
		{Object.entries(skillsData).map(([key, category], index) => (
			<div class="skill-group reveal-item" style={`--delay: ${index}`}>
				<h4 class="skill-group-title">{t(`skills.categories.${key}`, locale)}</h4>
				<div class="skill-badges">
					{category.skills.map((skill) => (
						<span class:list={[
							"skill-badge",
							skill.level >= 90 ? "badge-filled" : skill.level >= 80 ? "badge-secondary" : "badge-outline"
						]}>
							{skill.name}
						</span>
					))}
				</div>
			</div>
		))}
	</div>
</section>

<style>
	.skills {
		padding: var(--spacing-4xl) var(--spacing-lg);
		max-width: var(--wide-width);
		margin: 0 auto;
	}

	.section-label {
		font-size: var(--font-size-sm);
		font-weight: 500;
		color: var(--color-accent);
		text-transform: uppercase;
		letter-spacing: var(--tracking-wider);
		margin-bottom: var(--spacing-sm);
	}

	.section-title {
		font-family: var(--font-serif);
		font-size: var(--font-size-3xl);
		font-weight: 500;
		letter-spacing: var(--tracking-tight);
	}

	.section-header {
		margin-bottom: var(--spacing-2xl);
	}

	.skills-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--spacing-xl);
	}

	.skill-group {
		padding: var(--spacing-lg);
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
	}

	.skill-group-title {
		font-size: var(--font-size-sm);
		font-weight: 600;
		margin-bottom: var(--spacing-md);
		color: var(--color-text);
	}

	.skill-badges {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
	}

	.skill-badge {
		padding: 0.25rem 0.75rem;
		font-size: var(--font-size-xs);
		border-radius: 9999px;
		font-weight: 500;
	}

	.badge-filled {
		background: var(--color-text);
		color: var(--color-bg);
	}

	.badge-secondary {
		background: var(--color-bg-subtle);
		color: var(--color-text);
		border: 1px solid var(--color-border);
	}

	.badge-outline {
		background: transparent;
		color: var(--color-muted);
		border: 1px solid var(--color-border);
	}

	@media (max-width: 1024px) {
		.skills-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (max-width: 768px) {
		.skills-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
